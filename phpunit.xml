<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         requireCoverageMetadata="false"
         beStrictAboutCoverageMetadata="false"
         beStrictAboutOutputDuringTests="true"
         failOnRisky="true"
         failOnWarning="true"
         stopOnFailure="false">

    <!-- Test suites definition -->
    <testsuites>
        <testsuite name="WPInsight Test Suite">
            <directory>./tests/</directory>
            <exclude>./tests/bootstrap.php</exclude>
        </testsuite>
    </testsuites>

    <!-- Source files for coverage -->
    <source>
        <include>
            <directory suffix=".php">./includes</directory>
            <file>./cloudfest-wporgdownload.php</file>
        </include>
        <exclude>
            <directory>./vendor</directory>
            <directory>./tests</directory>
            <file>./uninstall.php</file>
        </exclude>
    </source>

    <!-- PHP configuration -->
    <php>
        <!-- Define constants for testing -->
        <const name="WP_TESTS_PHPUNIT_POLYFILLS_PATH" value="./vendor/yoast/phpunit-polyfills"/>

        <!-- WordPress test configuration (when WordPress test suite is installed) -->
        <!-- <const name="WP_TESTS_DIR" value="/tmp/wordpress-tests-lib"/> -->
        <!-- <const name="WP_CORE_DIR" value="/tmp/wordpress"/> -->

        <!-- Plugin testing constants -->
        <const name="WPINSIGHT_TESTING" value="true"/>

        <!-- Disable external HTTP requests during tests -->
        <const name="WP_HTTP_BLOCK_EXTERNAL" value="false"/>

        <!-- WordPress debug mode -->
        <const name="WP_DEBUG" value="true"/>
        <const name="WP_DEBUG_LOG" value="false"/>
        <const name="WP_DEBUG_DISPLAY" value="true"/>
    </php>

</phpunit>
